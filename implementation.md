Basic DB arch is already created,
Table-> Cars, UserSearch, recommendation, Reviews, RecommendedCars


will now move to features.
backend -> user gets a response of cars uplone visiting the home page
filters -> user will get filtered cars from backend
AI Search -> recommended cars from AI that will be matched with DB and return as response, only that are availble.

reviews, match scores and provide review option will also be included.


Fontend
home page, listing of cars
filtering of cars
personilized text search(AI) -> "i need a 4 seater car that is good for city and also have good mileage" -> AI checks, that cars will be checked against DB

